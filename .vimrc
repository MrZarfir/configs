" Line numbering always on
set number

" Use two spaces for indentation
set expandtab
set tabstop=2
set shiftwidth=2

" Theme & Syntax coloring
filetype on
syntax on
if (has("termguicolors"))
  set termguicolors
endif
